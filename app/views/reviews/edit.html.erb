<%= render 'form', product: @review.product, review: @review, product_id: @review.product_id  %>

<%= link_to 'Delete', product_review_path(@review.product, @review), method: :delete, data: {confirm: "Are you sure about this?"} %>
